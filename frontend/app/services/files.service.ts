import { Injectable } from "@angular/core";
import { ZhFile } from "./zh-form-tab8.models";

@Injectable({
  providedIn: "root",
})
export class FilesService {
  constructor() {}

  filterByExtension(files: Z);
}
